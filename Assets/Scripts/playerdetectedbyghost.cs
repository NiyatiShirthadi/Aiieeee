using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerdetectedbyghost : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
   /* void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.tag=="spookdetector")
        {
            other.gameObject.GetComponent<spookyghostbehavior>().hasdetected = true;
            Debug.Log("detect1");
        }
    }*/
}
